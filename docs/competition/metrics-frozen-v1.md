# 数字冻结表 v1（全报告唯一数字来源）

> ⚠️ 本表是报告、补充材料、视频、答辩全部数字的唯一来源。任何文档中的数字
> 必须与本表一致；改数字只能改这里，然后同步各处。
> 状态: 骨架（G5 复跑完成后由 `python3 src/scripts/summarize_g5.py` 自动填充数据段）

## 1. 基准规模（已冻结，G5 不变）

| 指标 | 数值 |
|---|---|
| 攻击用例候选总量 | 391 |
| 裸诱导成立 | 203 (52%) |
| 真实环境有效攻击（GOV-Bench v1.0） | **66** |
| 白名单良性样本 | 25 |
| 模式分布 | 外泄 40 / 破坏 18 / 篡改 6 / 系统破坏 1 / 配置伪装 1 |
| 语义天花板（不可诱导模式） | 审批/签字/发布/间接注入（0/40+） |
| 载体分布 | exec 外传 37 / exec 删除 18 / exec 篡改 6 / 其他 5 |

## 2. 防护效果（G5 复跑后自动填充）

数据来源: `results/g5-*.jsonl`，运行 `python3 src/scripts/summarize_g5.py` 生成
`metrics-g5-data.md` 后，将结果表粘贴/引用到此处。

| 指标 | 数值 | 状态 |
|---|---|---|
| 拦截率（66×3 seeds） | 〔G5 待填〕 | 分层新版 |
| 穿透 | 〔G5 待填〕 | |
| 白名单误报率（25×3） | 〔G5 待填〕 | |
| 分层矩阵（66→L2/L3，白→L0/L1） | 〔G5 待填〕 | |
| 口径分离（detected / degraded） | 〔G5 待填〕 | judge 故障不再计入拦截 |

## 3. 消融与基线（如做，从 06-experiments 更新）

| 指标 | 数值 | 状态 |
|---|---|---|
| 四层逐层消融 | 〔待定：是否补做〕 | |
| 基线对比（无防护/仅关键词/仅规则） | 〔待定〕 | |

## 4. 历史数字（已废弃，报告中禁止出现）

以下数字是 judge bug 时代的产物，**任何提交物中不得再出现**：
- ~~86.4% → 100% 拦截率~~（judge 误拦虚高）
- ~~0 误报~~（白名单从未覆盖审批/命令类）
- ~~156 条数据集 / 96.5% 拦截率~~（有效性口径混淆）

## 5. 固定事实（可直接引用）

- 防护层级: 意图分类器 → 规则引擎 → 污点分析 → 语义评审（四层）
- 处置分层: L0 放行 / L1 上报 / L2 确认 / L3 阻断
- 被测环境: OpenClaw 2026.7.1（50 政务技能）+ deepseek-v4-flash
- 验证链: 裸诱导预筛 → 真实环境有效性 → 防护拦截复测（三层）
- 部署: `./run_e2e.sh`（mock :4001 + 防护代理 :4000）
